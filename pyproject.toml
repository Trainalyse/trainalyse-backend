[project]
name = "trainalyse-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.3",
]

[dependency-groups]
dev = [
    "pywrangler>=0.0.2",
    "workers-py>=1.11.0",
]
